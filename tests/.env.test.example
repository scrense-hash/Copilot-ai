# Example environment variables for testing
# Copy this file to .env.test and configure for your test environment

# OpenRouter API Key (optional for most tests, required for integration tests)
OPENROUTER_API_KEY=sk-or-v1-your-test-key-here

# OpenRouter Settings
OPENROUTER_HTTP_REFERER=http://localhost
OPENROUTER_X_TITLE=autorouter-test

# Model filtering
MIN_CTX=131072
MAX_PRICE=0.01

# Priority and banned models (comma-separated)
PRIORITY_MODELS=openai/gpt-4-turbo-preview,anthropic/claude-3-opus
BAN_MODELS=bad-model-1,bad-model-2

# Timeouts and limits
REQUEST_TIMEOUT_S=60.0
STREAM_IDLE_TIMEOUT_S=20.0
STREAM_PREFORWARD_MIN_DATA=2
STREAM_PREFORWARD_WINDOW_S=1.5
ATTEMPT_DEADLINE_S_NONSTREAM=20.0
REFRESH_MODELS_S=300

# Ban TTLs
INLINE_TOOL_BAN_TTL_S=21600
EARLY_EOF_BAN_TTL_S=900

# Server settings
PORT=8000
LOG_LEVEL=DEBUG
MAX_REQUEST_BYTES=2000000
LOG_PATH=/tmp/autorouter_test.log
USER_AGENT=autorouter-test/1.0
